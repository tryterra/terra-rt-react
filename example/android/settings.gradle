pluginManagement {
  repositories {
    gradlePluginPortal()
    google()
    mavenCentral()
  }
  includeBuild("../node_modules/@react-native/gradle-plugin")
}

dependencyResolutionManagement {
  repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
  repositories {
    google()
    mavenCentral()
  }
}

rootProject.name = "TerraRtReactExample"
include(":app")

include(":react-native-terra-rt-react")
project(":react-native-terra-rt-react").projectDir = new File(rootProject.projectDir, "../../android")
